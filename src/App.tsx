/**
 * Side Hustle Simulator - Main App
 * 
 * Separate systems:
 * - Apartment (housing, rooms, hobby spaces)
 * - Hobbies (plants, mushrooms, etc.)
 * - Kitchen (shared storage for food)
 * - Economy (money flow)
 */

import React from 'react';
import { Game } from './Game';

export default function App() {
  return <Game />;
}
